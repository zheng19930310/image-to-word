# 百度AI API申请指南

本指南将帮助你免费申请百度AI开放平台的OCR API密钥，用于图片文字识别。

## 📋 申请步骤

### 第一步：注册百度账号

1. 访问百度AI开放平台：https://ai.baidu.com/
2. 点击右上角"登录"或"注册"
3. 如果没有百度账号，先注册一个（可以使用手机号或邮箱）
4. 登录后完成实名认证（个人用户即可）

### 第二步：创建应用

1. 进入控制台：https://console.bce.baidu.com/ai/
2. 在左侧菜单找到"文字识别" → "应用列表"
3. 点击"创建应用"按钮
4. 填写应用信息：
   - **应用名称**：例如 "ImageToWord"（随意填写）
   - **应用描述**：例如 "个人学习使用的图片转Word工具"
   - **应用类型**：选择"个人"
   - **应用场景**：选择"其他"或"教育/学习"
5. 点击"立即创建"

### 第三步：获取API密钥

创建成功后，你会看到应用详情页面，包含三个重要信息：

- **App ID**：应用ID（一串数字）
- **API Key**：API密钥
- **Secret Key**：密钥

**请复制这三个值，后续需要配置到项目中。**

### 第四步：配置到项目

打开项目中的 `config.py` 文件，找到以下部分：

```python
BAIDU_OCR = {
    'APP_ID': 'your_app_id',          # 替换为你的App ID
    'API_KEY': 'your_api_key',        # 替换为你的API Key
    'SECRET_KEY': 'your_secret_key',  # 替换为你的Secret Key
}
```

将刚才复制的三个值填入对应位置，例如：

```python
BAIDU_OCR = {
    'APP_ID': '12345678',
    'API_KEY': 'abcdefg123456',
    'SECRET_KEY': 'xyz789abc123',
}
```

保存文件即可。

## 💰 免费额度说明

### 通用文字识别（高精度版）
- **免费额度**：每天 50,000 次调用
- **适用场景**：个人学习完全够用
- **超出后**：可以付费继续使用，或第二天重置

### 表格文字识别
- **免费额度**：每天 500 次调用
- **适用场景**：处理表格文档

### 文档结构化识别
- **免费额度**：每月 1,000 次调用
- **适用场景**：复杂版面分析

**对于个人学习使用，每天的免费额度绰绰有余！**

## 🔒 安全提示

1. **不要公开你的API密钥**：不要将 `config.py` 文件上传到公开的代码仓库
2. **定期更换密钥**：如果怀疑密钥泄露，可以在控制台重新生成
3. **监控使用情况**：在控制台可以查看API调用统计

## ❓ 常见问题

### Q1: 申请需要多长时间？
A: 通常即时开通，实名认证可能需要几分钟审核。

### Q2: 是否需要付费？
A: 个人学习使用完全免费，每天有充足的免费额度。

### Q3: 如果免费额度用完了怎么办？
A: 
- 等待第二天自动重置
- 或者在控制台申请提高额度
- 也可以付费购买更多调用次数

### Q4: API有调用频率限制吗？
A: 有，但个人使用不会触及限制。默认QPS（每秒查询率）为2，足够使用。

### Q5: 支持哪些图片格式？
A: 支持 JPG、PNG、BMP 等常见格式。

### Q6: 图片大小有限制吗？
A: 单张图片不超过4MB，建议分辨率在1000-3000像素之间。

## 📞 技术支持

如果遇到问题，可以：
1. 查看百度AI官方文档：https://ai.baidu.com/ai-doc/OCR
2. 在百度AI社区提问：https://ai.baidu.com/forum
3. 联系百度客服

## ✅ 验证配置是否成功

配置完成后，运行以下命令测试：

```bash
python baidu_ocr.py input/test.jpg
```

如果看到识别结果输出，说明配置成功！

---

**祝你使用愉快！如有问题欢迎反馈。**
